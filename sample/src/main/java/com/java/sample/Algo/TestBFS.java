package com.java.sample.Algo;


import java.math.BigDecimal;

/**
 *
 */
public class TestBFS {

    //對一個節點進行廣度優先遍歷的方法 (是否被遍歷,對哪個節點進行廣度遍歷)
    private void bfs(boolean[] isVisited,int i){
        int u ; //表示對列的頭節點對應下標
        int w; //旁邊的節點w
        //對列;記錄節點訪問的順序
    }

    public static void main(String[] args) {
        BigDecimal v2 =new BigDecimal("5E+490096965");
        BigDecimal v1 =BigDecimal.valueOf(-4297);
        BigDecimal result=v2.subtract(v1);
        System.out.println("result:"+result);


    }

}
